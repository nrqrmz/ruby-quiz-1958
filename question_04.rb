def multiply(x, y, z = 10) # definimos el método multiply con 2 parametros (x, y)
  return x * y * z    # retornamos el resultado de multiplicar los parametros x , y
end                # cerramos la definicion del metodo

puts multiply(5, 8, 2) # invocamos/llamamos el metodo y lo imprimimos
